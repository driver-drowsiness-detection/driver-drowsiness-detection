# Model assets

Arushuuu maintains model provenance and CNN runtime loading. Vishruth reviews preprocessing, score calibration and integration. Both seniors review deployment compatibility.

landmarks holds localisation assets; checkpoints holds training weights; calibration holds score mappings and selected settings; exports holds compatible runtime exports.

These directories retain markers in Git but ignore their contents. Record source URL, licence, filename, checksum, acquisition command, version, input size, colour order, normalisation and class mapping here when each asset is approved and tested.

No model assets have been downloaded or trained.
